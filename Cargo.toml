[package]
name = "oxicloud"
version = "0.6.0"
edition = "2021"
# (other fields as applicable)

[dependencies]
# existing dependencies here...

# Add oxc_semantic dependency
oxc_semantic = "0.125.0"

# (other dependencies)
