diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index e66e91c..a90222b 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -37,6 +37,7 @@ jobs:
               - 'jsconfig.json'
             backend:
               - 'src/**'
+              - 'xtask/**'
               - 'Cargo.toml'
               - 'Cargo.lock'
               - 'tests/**'
@@ -169,6 +170,10 @@ jobs:
     steps:
       - uses: actions/checkout@v4
 
+      - uses: dtolnay/rust-toolchain@stable
+
+      - uses: Swatinem/rust-cache@v2
+
       - uses: actions/download-artifact@v4
         with:
           name: oxicloud-release
@@ -188,9 +193,12 @@ jobs:
           sudo apt-get install -y "./hurl_${HURL_VERSION}_amd64.deb" b3sum
 
       - name: Run Hurl API tests
-        run: bash tests/api/run.sh
+        run: cargo run -p xtask -- test --tests-dir tests/api --timeout-secs 30 --strict
         env:
           BUILD_TARGET: release
+          OXICLOUD_BIN: target/release/oxicloud
+          DATABASE_URL: postgres://oxicloud_test:oxicloud_test@localhost:5433/oxicloud_test
+          OXICLOUD_DB_CONNECTION_STRING: postgres://oxicloud_test:oxicloud_test@localhost:5433/oxicloud_test
 
       - name: Run Webdav tests
         run: bash tests/webdav/run.sh
